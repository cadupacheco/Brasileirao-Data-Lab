[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "brasileirao-data-lab"
version = "0.1.0"
description = "Coleta e análise de dados do Campeonato Brasileiro"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]