name: Automated Brasileirão Update

on:
  schedule:
    # Horários em UTC.
    #
    # Brasília (UTC-3):
    # 08:00 -> 11:00 UTC
    # 14:00 -> 17:00 UTC
    # 20:00 -> 23:00 UTC
    # 23:59 -> 02:59 UTC do dia seguinte
    #
    # O workflow roda todos os dias.
    # Se a CBF não mudou, nada é alterado.

    - cron: "0 11 * * *"
    - cron: "0 17 * * *"
    - cron: "0 23 * * *"
    - cron: "59 2 * * *"

  # Permite executar manualmente pela aba Actions.
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: automated-brasileirao-update
  cancel-in-progress: false


jobs:
  update:
    name: Check and Update Brasileirão

    # A CBF apresenta incompatibilidade de validação
    # da cadeia SSL no runner Linux.
    #
    # O scraper utiliza truststore, que no Windows
    # consegue validar corretamente o certificado da CBF.
    runs-on: windows-latest

    # Força o Python a utilizar UTF-8 no runner Windows.
    env:
      PYTHONUTF8: "1"
      PYTHONIOENCODING: "utf-8"

    # Mantemos Bash para que os comandos do workflow
    # sejam iguais em todos os passos.
    defaults:
      run:
        shell: bash

    steps:

      # =======================================================================
      # Checkout
      # =======================================================================

      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0


      # =======================================================================
      # Python
      # =======================================================================

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"
          cache: pip
          cache-dependency-path: requirements.txt


      # =======================================================================
      # Dependências
      # =======================================================================

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Install project
        run: pip install -e .


      # =======================================================================
      # Atualização principal
      # =======================================================================

      - name: Check CBF and update project
        run: |
          python -m brasileirao_data_lab.pipelines.automated_project_update


      # =======================================================================
      # Detecta mudanças nos dados principais
      # =======================================================================

      - name: Detect core data changes
        id: core_changes
        run: |
          if git diff --quiet -- \
            data/brasileirao.db \
            data/processed/matches.csv \
            data/ml/matches_history.csv \
            data/ml/features.csv \
            data/ml/future_predictions.csv \
            data/ml/season_simulation.csv
          then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "Nenhuma mudanca nos dados principais."
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "Mudanca real detectada nos dados."
          fi


      # =======================================================================
      # Metadata
      # =======================================================================

      - name: Build update metadata
        if: steps.core_changes.outputs.changed == 'true'
        run: |
          python scripts/build_update_metadata.py


      # =======================================================================
      # Detecta tudo que será publicado
      # =======================================================================

      - name: Detect generated changes
        id: changes
        run: |
          if git diff --quiet; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "Nenhuma alteracao encontrada."
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "Alteracoes encontradas."
            git status --short
          fi


      # =======================================================================
      # Testes
      # =======================================================================

      - name: Run Python tests
        if: steps.changes.outputs.changed == 'true'
        run: pytest -q


      # =======================================================================
      # Mostra exatamente o que será publicado
      # =======================================================================

      - name: Show files to publish
        if: steps.changes.outputs.changed == 'true'
        run: |
          echo "Arquivos alterados:"
          git diff --name-only


      # =======================================================================
      # Git
      # =======================================================================

      - name: Configure Git
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "brasileirao-data-lab-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"


      # =======================================================================
      # Commit
      #
      # Alguns artefatos oficiais são ignorados pelo .gitignore
      # durante o desenvolvimento local.
      #
      # Como este workflow é o publicador oficial desses arquivos,
      # usamos -f exclusivamente na lista autorizada abaixo.
      # =======================================================================

      - name: Commit updated data
        if: steps.changes.outputs.changed == 'true'
        run: |
          git add -f \
            data/brasileirao.db \
            data/processed/matches.csv \
            data/ml/matches_history.csv \
            data/ml/features.csv \
            data/ml/future_predictions.csv \
            data/ml/season_simulation.csv \
            data/update_metadata.json

          echo "Arquivos preparados para commit:"
          git status --short

          git commit -m "data: atualiza Brasileirao automaticamente"


      # =======================================================================
      # Push
      # =======================================================================

      - name: Push updated data
        if: steps.changes.outputs.changed == 'true'
        run: |
          git push origin HEAD:main


      # =======================================================================
      # Resumo
      # =======================================================================

      - name: No update required
        if: steps.changes.outputs.changed == 'false'
        run: |
          echo "CBF sem alteracoes."
          echo "Nenhum modelo foi recalculado."
          echo "Metadata permaneceu inalterado."
          echo "Nenhum commit foi criado."


      - name: Update completed
        if: steps.changes.outputs.changed == 'true'
        run: |
          echo "Dados atualizados."
          echo "Metadata atualizado."
          echo "Testes aprovados."
          echo "Commit criado."
          echo "Atualizacao enviada para main."